import type { ReactNode } from "react";

type SummaryCardsDataType = {
  label: string;
  value: number;
  detail: string;
  icon: ReactNode;
  tone: string;
};

export type { SummaryCardsDataType };
